const printMyNameWithArrowFunction = (name) =>
{
    console.log(name);
}

printMyNameWithArrowFunction('Lucas with arrow functions');

function printMyNameWithoutArrowFunction(name)
{
    console.log(name);
}

printMyNameWithoutArrowFunction('Lucas without arrow functions');